<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="?attr/md3Surface"
    android:padding="32dp">

    <!-- 大号圆形头像（显示对方名首字符） -->
    <TextView
        android:id="@+id/tvCallerAvatar"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:gravity="center"
        android:tag="md3-circle-primary"
        android:textColor="?attr/md3OnPrimaryContainer"
        android:textSize="44sp"
        android:textStyle="bold"
        android:text="?" />

    <TextView
        android:id="@+id/tvCallerName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:textColor="?attr/md3OnSurface"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="16dp"
        android:text="通话中" />

    <TextView
        android:id="@+id/tvDuration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="40sp"
        android:textColor="?attr/md3OnSurfaceVariant"
        android:fontFamily="monospace"
        android:layout_marginBottom="56dp"
        android:text="00:00" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="32dp">

        <Button
            android:id="@+id/btnSpeaker"
            android:layout_width="wrap_content"
            android:layout_height="64dp"
            android:tag="md3-btn-tonal"
            android:text="@string/call_speaker"
            android:drawableLeft="@drawable/ic_md3_volume_up"
            android:drawablePadding="8dp"
            android:paddingLeft="24dp"
            android:paddingRight="24dp"
            android:layout_marginEnd="24dp" />

        <Button
            android:id="@+id/btnHangUp"
            android:layout_width="wrap_content"
            android:layout_height="64dp"
            android:tag="md3-btn-danger"
            android:text="@string/call_hangup"
            android:drawableLeft="@drawable/ic_md3_call_end"
            android:drawablePadding="8dp"
            android:textSize="16sp"
            android:textStyle="bold"
            android:paddingLeft="32dp"
            android:paddingRight="32dp" />
    </LinearLayout>

</LinearLayout>
